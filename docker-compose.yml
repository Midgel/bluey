services:
    app:
        build: 
            context: .
            dockerfile: Dockerfile
        container_name: bluey
        restart: always
        volumes:
            - .:/var/www/html
  
    web:
        build:
            context: ./nginx-docker
            dockerfile: Dockerfile
        container_name: bluey-web
        restart: always
        ports:
            - "80:80"
        volumes:
            - .:/var/www/html

